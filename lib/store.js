const shortid = require('shortid');

module.exports = class Store {
    constructor(root) {
        this.root = root;
    }
    saveFile(data) {


        ``
    }
    // newAnimal() {
    //     this.
    // }
};